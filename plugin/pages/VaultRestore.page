Title="Restore"
Tabs="true"
Icon="shield"
Markdown="false"
---
<?php
require_once '/usr/local/emhttp/plugins/vault/include/api.php';
$jobs = vault_get('/jobs') ?: [];
?>

<link rel="stylesheet" href="/plugins/vault/assets/css/vault.css">

<div class="vault-page">
    <h1>Restore</h1>
    <p>Select a backup job to browse its restore points.</p>

    <div class="vault-form-group">
        <label>Select Job</label>
        <select id="restoreJobSelect" onchange="loadRestorePoints(this.value)">
            <option value="">-- Select a job --</option>
            <?php foreach ($jobs as $job): ?>
            <option value="<?= $job['id'] ?>"><?= htmlspecialchars($job['name']) ?></option>
            <?php endforeach; ?>
        </select>
    </div>

    <table class="vault-table" id="restorePointsTable" style="display:none;">
        <thead>
            <tr>
                <th>Date</th>
                <th>Type</th>
                <th>Size</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody id="restorePointsBody"></tbody>
    </table>
</div>

<script src="/plugins/vault/assets/js/vault.js"></script>
<script src="/plugins/vault/assets/js/restore.js"></script>
